name = "adrian-website"
compatibility_date = "2024-09-23"
# Better Auth (self-owned customer login) uses node:crypto, which Cloudflare
# only exposes behind this flag. Required for the /api/auth/* handler.
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[[r2_buckets]]
binding = "MUSIC_BUCKET"
bucket_name = "adrian-music"

# D1 database for user accounts, profiles, orders, cart, collections, and invoices.
# Provisioned 2026-06-05 via:
#   npx wrangler d1 create adrian-website
#   npx wrangler d1 migrations apply adrian-website --remote
[[d1_databases]]
binding = "DB"
database_name = "adrian-website"
database_id = "d0e93f04-203c-4dbd-945a-e14a9a364dd5"
migrations_dir = "migrations"
